# Configuration

Configuration is kept separate from source so mission-specific values do not become scattered constants.

- `missions/` contains one human-readable reference file per mission, beginning with `HAB-001`.
- `sensors/` records selected models, interfaces, ranges, and assumptions as they become known.

The initial files are documentation/configuration references. There is no runtime configuration parser yet.
