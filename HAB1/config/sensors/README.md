# Sensor configuration notes

Record the selected part number, library, interface, voltage, range, sample rate, calibration notes, and validity limits for each sensor here or in a linked sensor-specific document.

Until a model is approved, use `TBD` rather than guessing a driver or pin map.
