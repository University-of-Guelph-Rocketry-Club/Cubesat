# Ground telemetry

Planned responsibilities include receiving packets, checking packet versions and validity, decoding the shared telemetry record, writing received packets to disk, and displaying the latest position and system health.

No radio or link protocol has been selected yet. Keep the first implementation as a small command-line receiver/decoder rather than making a GUI a prerequisite.
