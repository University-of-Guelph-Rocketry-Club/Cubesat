# Data policy

Only small example datasets required for documentation or tests belong in Git. Real flight logs, raw radio captures, and generated analysis outputs may become large and should be stored outside the repository or under ignored paths such as `data/raw/` and `data/recordings/`.

The files in `sample/` are synthetic fixtures, not evidence from a real flight.
