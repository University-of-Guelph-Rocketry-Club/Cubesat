# Unit tests

Planned hardware-free tests include altitude calculations, unit conversions, GPS parsing, telemetry encoding/decoding, checksum/CRC, and mission-state transitions.

Each test should include explicit units and should verify invalid-input behavior as well as nominal values.
