/* Standalone GPS bring-up application. */

int main(void)
{
    /* TODO: initialize the STM32 board, UART, and reusable GPS module. */
    for (;;) {
        /* TODO: poll the receiver and expose valid fix information. */
    }
}
