/* Standalone STM32-to-Pi link bring-up application. */

int main(void)
{
    /* TODO: initialize the board and reusable Pi-link transport module. */
    for (;;) {
        /* TODO: send a bounded test frame without blocking indefinitely. */
    }
}
