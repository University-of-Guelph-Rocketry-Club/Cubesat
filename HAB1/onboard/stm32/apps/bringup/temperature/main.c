/* Standalone temperature-sensor bring-up application. */

int main(void)
{
    /* TODO: initialize the board and reusable temperature module. */
    for (;;) {
        /* TODO: poll, validate, and expose temperature_c. */
    }
}
