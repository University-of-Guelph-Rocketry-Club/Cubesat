# Temperature bring-up

Student workspace for reading the selected temperature sensor in degrees Celsius and exposing invalid or failed reads visibly.

Sensor model, interface, pins, and operating limits remain `TBD`.
