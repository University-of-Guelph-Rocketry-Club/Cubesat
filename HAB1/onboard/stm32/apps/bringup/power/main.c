/* Standalone power-monitor bring-up application. */

int main(void)
{
    /* TODO: initialize the board ADC and reusable power module. */
    for (;;) {
        /* TODO: poll, validate, and expose battery_voltage_v. */
    }
}
