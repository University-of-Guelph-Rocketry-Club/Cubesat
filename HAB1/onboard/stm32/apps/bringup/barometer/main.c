/* Standalone barometer bring-up application. */

int main(void)
{
    /* TODO: initialize the STM32 board and reusable barometer module. */
    for (;;) {
        /* TODO: poll, validate, and expose pressure_pa. */
    }
}
