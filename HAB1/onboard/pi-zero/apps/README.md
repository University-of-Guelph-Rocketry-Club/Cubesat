# Pi Zero applications

- `data_logger/` is the final integrated logger/camera application.
- `bringup/` contains focused camera, STM32-link, and storage exercises.

Reusable behavior belongs in `modules/`, not copied between applications.
