# Onboard integration

Integration assembles the final STM32 flight application, Pi Zero data logger, canonical protocols, mission configuration, wiring, and rehearsal procedures. It does not contain copied subsystem implementations.

`HAB-001/` identifies the exact applications and outstanding hardware decisions for the first mission. `test-rigs/` describes end-to-end bench setups.
