# Pi Camera wiring

The Pi Camera is expected to use the Pi camera connector. Record the exact Pi and camera models, cable orientation, retention method, environmental limits, and capture test results before integration.

The camera must be treated as a Pi subsystem; it must not affect STM32 flight-controller startup or execution.
