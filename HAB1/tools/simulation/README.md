# Telemetry simulation

Planned lightweight simulator sequence:

```text
ground → ascent → burst → descent → landing
```

It should generate plausible timing and state transitions without attempting a sophisticated atmospheric model.
